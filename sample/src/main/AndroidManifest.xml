<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.deange.ropeprogress">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">

        <activity
                android:name=".ChooserActivity"
                android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".StaticHeightRopeActivity" android:label="@string/activity_static"/>
        <activity android:name=".DynamicHeightRopeActivity" android:label="@string/activity_dynamic"/>
        <activity android:name=".ThemedRopeActivity" android:label="@string/activity_themed"/>
        <activity android:name=".CustomTextRopeActivity" android:label="@string/activity_custom"/>

    </application>

</manifest>
